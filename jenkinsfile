pipeline {
    agent {
        label 'agent1'
    }

    stages {
        
        stage('Build') {
            steps {
		sh 'sudo docker build -t bm-app -f ./escape_room-Q-order/dockerfile ./escape_room-Q-order'            
            }
        }
    }
    post {
        always {
            // Clean up the workspace
            cleanWs()
               
           
        }
    }
}

