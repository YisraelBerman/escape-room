pipeline {
    agent {
	    label 'dynamicagent'
    }
    stages {
        
        stage('Build') {
            steps {
		sh 'docker build -t bm-app -f ./escape_room-Q-order/dockerfile ./escape_room-Q-order'            
            }
        }
    }
    post {
        always {
            // Clean up the workspace
            cleanWs()
               
           
        }
    }
}

