pipeline {
    agent any

    stages {
        
        stage('Build') {
            steps {
                // Run a simple shell command
                sh 'echo "Hello, Jenkins! this is a test"'
            }
        }
    }
    post {
        always {
            // Clean up the workspace
            cleanWs()
               
           
        }
    }
}

